defaults:
  - training@training
  - evaluation@evaluation
  - model@model
  - dataset_CHO@dataloaders
  - _self_

runner:
  _target_: runner.BaseMethod
  model: ${model}
  dataloaders: ${dataloaders}
  train_args: ${training}
  val_args: ${evaluation}
